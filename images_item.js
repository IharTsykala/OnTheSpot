if (jQuery('.Carousel__Cell.is-selected img:first').length > 0) 
    $img = jQuery('.Carousel__Cell.is-selected img:first').attr('src')
else
    $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
  'https:'+$img
else
  $img

  document.querySelector('.gallery-thumbs a').src

  if (jQuery(".Product__SlideshowNavScroller a").length > 0){
    $arr = [];
   jQuery(".Product__SlideshowNavScroller a").each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).find('img:first').attr('src'))
    })
   $arr
 }

 m-tp-productimagegallery-preview-wrapper

 if (jQuery('.slick-active img:first').length > 0) 
    $img = 
    'https:' +
     jQuery('.slick-active noscript')
     .text().trim()
     .split('src="')[1]
     .split(' alt')[0]
     .trim()
     .slice(0, -1)
    // .attr('data-srcset')
    // .split(' ')[0]
    // .replace('_85x', '_1000x')
    // .css('background-image').split('url("')[1].slice(0, -2)
else
    $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
  'https:'+$img
else
  $img
  
  // .css('background-image').split('url("')[1].slice(0, -2)

  if (jQuery('.slick-active img').length > 0){
    $arr = [];
   jQuery('.slick-active img').each(function(index){
        if (index < 4) 
        $arr.push(('https:' + jQuery(this).attr('data-srcset')).split(' ')[0])
    })
   $arr
 }

 owl-lazy img-responsive wrapper-main-slider__image lazy-init

 //////////////////////

 if (jQuery('.js-zoom-enabled [role="presentation"]:first').length > 0) 
 $img =
  // 'https://www.perfectlens.ca' +
   jQuery('.js-zoom-enabled [role="presentation"]:first').attr('src')
else
 $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img
///////////////////////////////////////////

if (jQuery('[role="presentation"]').length > 0){
 $arr = [];
jQuery('[role="presentation"]').each(function(index){
     if (index < 4) 
     $arr.push(jQuery(this).attr('src'))
 })
$arr
}
/////////////////////////

if (jQuery('.swiper-slide-active img:first').length > 0) 
$img = jQuery('.swiper-slide-active img:first').attr('src')
.split(' ')[0]
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img

if (jQuery('.swiper-slide-active img').length > 0){
$arr = [];
jQuery('.swiper-slide-active img').each(function(index){
    if (index < 4) 
    $arr.push(jQuery(this).attr("src").split(' ')[0])
})
$arr
}

//////////////////////////////
pdp-main-img primary-images col-12

let img
if (document.querySelectorAll("._3KsTU0 img._30XEf0").length > 0) 
img = document.querySelector("._3KsTU0 img._30XEf0").src
else if(document.querySelectorAll(".bJCmFu img.cydMly").length > 0)
img =document.querySelector(".bJCmFu img.cydMly").src
else
img = document.querySelector('meta[property="og:image"]:eq(0)').getAttribute('content')

if (img.indexOf("http") == -1)
'https:'+img
else
img

if (jQuery("._3KsTU0 img._30XEf0").length > 0) 
img = jQuery("._3KsTU0 img._30XEf0").attr('src')
else if(jQuery(".bJCmFu img.cydMly").length > 0)
img =jQuery(".bJCmFu img.cydMly").attr('src')
img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img

document.querySelector('._3KsTU0 img._30XEf0').src

if (jQuery('ul.LzhdeS li').length > 0){
$arr = [];
jQuery('ul.LzhdeS li').each(function(index){
    if (index < 4) 
    $arr.push(jQuery(this).find('._2_AcLJ').css('background-image'))
    // .split('"')[1])
}
$arr
})

document.querySelectorAll('._3KsTU0 img._30XEf0')

// product__thumbs product__thumbs--below product__thumbs-placement--left small--hide aos-init slick-initialized slick-slider aos-animate

// native
// 
let containerUl = Array.from(document.querySelectorAll('ul.LzhdeS li ._2_AcLJ'))

if(containerUl.length > 4) containerUl.length = 4
containerUl.map(item=>item.getAttribute('style', "background-image").slice(21, -1))


if (jQuery(".primary-image-carousel-container .slick-active img:first").length > 0) 
$img =  jQuery(".primary-image-carousel-container .slick-active img:first").attr("src")
// jQuery(".zoomContainer .zoomWindow:first").css('background-image').split('"')[1] 
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img


if (jQuery('.zoomWindowContainer div:first').length > 0) 
$img = jQuery(".zoomWindowContainer div:first").css('background-image').slice(5, -2)
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img

/////////////////////////////////////////////////////////////////////////////

if (jQuery('[data-layout-name="classic"] .slick-active [data-hook="product-image"]:first').length > 0) 
$img = jQuery('[data-layout-name="classic"] .slick-active [data-hook="product-image"]:first').attr('src').replace(/\s/g,'%20')    
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img

///////////////////////////////////////

if (jQuery('[data-hook="thumbnails-container"]  [data-hook="thumbnail-image"]').length > 0){
$arr = [];
jQuery('[data-hook="thumbnails-container"]  [data-hook="thumbnail-image"]').each(function(index){
    if (index < 4) 
    $arr.push(jQuery(this)
    // .find('img:first')
    .attr("src")
    .replace(/\s/g,'%20')  )  
})
$arr
}

//////////////////////////////////////////////////////


/////////////////////////////////////////////////////////////////////////////

if (jQuery('[data-hook="product-image"]:first').length > 0) 
$img = jQuery('[data-hook="product-image"]:first').attr('src').replace(/\s/g,'%20')    
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img

///////////////////////////////////////

if (jQuery('[data-hook="product-image"]').length > 0){
$arr = [];
jQuery('[data-hook="product-image"]').each(function(index){
    if (index < 4) 
    $arr.push(jQuery(this)
    // .find('img:first')
    .attr("src")
    .replace(/\s/g,'%20')  )  
})
$arr
}

/////////////////////////////////////////////////////

product__thumbnail slick-slide slick-current slick-active
product_gallery_nav 


class="  lazyload "

if (jQuery('.product-image-zoom img:first').length > 0) 
$img = 
// 'https://www.deluxdeco.co.uk/' + 
jQuery('.product-image-zoom img:first').attr('src')
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img


if (jQuery('.product-single__photo.slick-active img').length > 0) 
 $img =
  // 'https://www.perfectlens.ca' +
   jQuery('.product-single__photo.slick-active img').attr('src')
else
 $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img

if (jQuery('.product-single__thumbnails img').length > 0){
  $arr = [];
  jQuery('.product-single__thumbnails img').each(function(index){
      if (index < 4) 
      $arr.push('https:'+jQuery(this).attr("src").split(' ')[0])
  })
  $arr
  }


if (jQuery('.bxslider img:first').length > 0) 
    $img = 
    'https:' +
     jQuery('.bxslider img:first')
    .attr('src')
    // .split(' ')[0]
    // .css('background-image').split('url("')[1].slice(0, -2)
else
    $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
  'https:'+$img
else
  $img

  //////////////////////////////

  if (jQuery('.product-single__thumbnails img').length > 0){
    $arr = [];
    jQuery('.product-single__thumbnails img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr("src").split(' ')[0])
    })
    $arr
    }


  ///////////////////////////////////


  if (jQuery('.zoomWindowContainer div:first').length > 0) 
    $img = 
    // 'https:' +
     jQuery('.zoomWindowContainer div:first')
    // .attr('src')
    // .split(' ')[0]
    // .replace('_85x', '_1080x')
    .css('background-image').split('url("')[1].slice(0, -2)
else
    $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
  'https:'+$img
else
  $img

  if (jQuery('ul[id="smallGallery"] img').length > 0){
    $arr = [];
    jQuery('ul[id="smallGallery"] img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }



  if (jQuery('.product-single__photos img:first').length > 0) 
  $img = jQuery('.product-single__photos img:first').attr('src')  
  .split(' ')[0]
  .replace('_200x200', '_1080x1080')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img


if (jQuery('.product-single__photos img').length > 0){
  $arr = [];
  jQuery('.product-single__photos img').each(function(index){
      if (index < 4) 
      $arr.push('https:'+jQuery(this).attr("src").split(' ')[0])
  })
  $arr
  }
/////////////////////////////////////////////////
////////////////////////////////////////////////////

if (jQuery('.slides li:first').length > 0) 
$img = jQuery('.slides li:first').find('img:first').attr('src')
// .split(' ')[0]
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('content')


if ($img.indexOf("http") == -1)
'https:'+$img
else
$img


if (jQuery('.slides li').length > 0){
  $arr = [];
  jQuery('.slides li').each(function(index){
      if (index < 4) 
      $arr.push('https:'+jQuery(this).find('img:first').attr("src"))
  })
  $arr
  }

//////////////////////

if (jQuery('.ProductItem-gallery-slides-item img:first').length > 0) 
$img =
// 'https:' +
jQuery('.ProductItem-gallery-slides-item img:first').attr('src')
// .split(' ')[0]
else
$img =
 jQuery('meta[property="og:image"]:eq(0)').attr('content')


if ($img.indexOf("http") == -1)
'https:'+$img
else
$img


if (jQuery('.ProductItem-gallery-thumbnails-item img').length > 0){
  $arr = [];
  jQuery('.ProductItem-gallery-thumbnails-item img').each(function(index){
      if (index < 4) 
      $arr.push(jQuery(this).attr("src"))
  })
  $arr
  }

  if (jQuery('.details-gallery__image-wrapper-inner img.details-gallery__picture:first').length > 0) 
$img = jQuery('.details-gallery__image-wrapper-inner img.details-gallery__picture:first').attr('src')
// .replace('=100', '=1050')
.split(', ')[0]
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('src')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img


if (jQuery('.details-gallery__image-wrapper-inner img.details-gallery__picture').length > 0){
  $arr = [];
  jQuery('.details-gallery__image-wrapper-inner img.details-gallery__picture').each(function(index){
      if (index < 4) 
      $arr.push(jQuery(this).attr("src"))
  })
  $arr
  }

  ////////////////////////////////

  if (jQuery('[class="swiper-slide swiper-slide-active"] img:first').length > 0) 
  $img = jQuery('[class="swiper-slide swiper-slide-active"] img:first')  
  .attr('src') 
  .split(' ')[0]
  .replace('_180', '_1080')
  else
  $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img  
  
  if (jQuery('[class="swiper-slide swiper-slide-active"] img').length > 0){
    $arr = [];
    jQuery('[class="swiper-slide swiper-slide-active"] img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr("src"))
    })
    $arr
    }

  //////////////////////////////////////
  ///////////////////////
  
  if (jQuery('.swiper-slide-active img:first').length > 0) 
  $img = (jQuery('.swiper-slide-active img:first').attr('src') || jQuery('.fotorama__active img:first').attr('src'))
  .replace(/\s/g,'%20')
  .split(', ')[0]
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  // New

  /////////////////////////////
  /////////////////////////////////////////////

  if (jQuery('.product__image-wrapper img:first').length > 0) 
  $img = jQuery('.product__image-wrapper img:first').attr('data-zoom')  
  .split(' ')[0]
  // .replace('180x', '1080x')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.product-photo-thumbs img').length > 0){
    $arr = [];
    jQuery('.product-photo-thumbs img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////


     /////////////////////////////////////////////

  if (jQuery('.product-image--cell img:first').length > 0) 
  $img = jQuery('.product-image--cell img:first').attr('srcset')  
  .split(' ')[0]
  .replace('_85', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.product-image--cell img').length > 0){
    $arr = [];
    jQuery('.product-image--cell img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('srcset')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

         /////////////////////////////////////////////

  if (jQuery('.product-single__photos img:first').length > 0) 
  $img = jQuery('.product-single__photos img:first').attr('src')  
  .split(' ')[0]
  .replace('_200x200', '_1080x1080')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.product-single__photos img').length > 0){
    $arr = [];
    jQuery('.product-single__photos img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////


    if (jQuery('[class="lazy-image product__photo slick-slide slick-current slick-active"] img:first').length > 0) 
    $img = jQuery('[class="lazy-image product__photo slick-slide slick-current slick-active"] img:first')  
    .attr('data-src') 
    .split(' ')[0]
    .replace('_180', '_1080')
    else
    $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
    
    if ($img.indexOf("http") == -1)
    'https:'+$img
    else
    $img
  
  
    
    
    if (jQuery('[class="lazy-image product__photo slick-slide slick-current slick-active"] img').length > 0){
      $arr = [];
      jQuery('[class="lazy-image product__photo slick-slide slick-current slick-active"] img').each(function(index){
          if (index < 4) 
          $arr.push('https:'+jQuery(this).attr("data-src"))
      })
      $arr
      }
  
    //////////////////////////////////////
    ///////////////////////

           /////////////////////////////////////////////

  if (jQuery('.product-photo-container img:first').length > 0) 
  $img = jQuery('.product-photo-container img:first').attr('src')  
  .split(' ')[0]
  .replace('_60', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.product-photo-container img:first').length > 0){
    $arr = [];
    jQuery('.product-photo-container img:first').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

            /////////////////////////////////////////////product-gallery__image is-selected

  if (jQuery('.woocommerce-product-gallery__image img:first').length > 0) 
  $img = jQuery('.woocommerce-product-gallery__image img:first').attr('src')  
  .split(' ')[0]
  .replace('_60', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.woocommerce-product-gallery__image img').length > 0){
    $arr = [];
    jQuery('.woocommerce-product-gallery__image img').each(function(index){
        if (index < 4) 
        $arr.push(jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

                /////////////////////////////////////////////

  if (jQuery('.product-images__list img:first').length > 0) 
  $img = jQuery('.product-images__list img:first').attr('src')  
  .split(' ')[0]
  .replace('_60', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.product-images__list img').length > 0){
    $arr = [];
    jQuery('.product-images__list img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

                    /////////////////////////////////////////////

  if (jQuery('#product .image img:first').length > 0) 
  $img = jQuery('#product .image img:first').attr('src')  
  .split(' ')[0]
  .replace('_60', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('#product .image img').length > 0){
    $arr = [];
    jQuery('#product .image img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

                        /////////////////////////////////////////////

  if (jQuery('.product-image-main img:first').length > 0) 
  $img = jQuery('.product-image-main img:first').attr('src')  
  .split(' ')[0]
  .replace('_360', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.product-image-main img').length > 0){
    $arr = [];
    jQuery('.product-image-main img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

                            /////////////////////////////////////////////

  if (jQuery('.product-single-slide').length > 0) 
  $img = jQuery('.product-single-slide:first').next().find('img:first').attr('srcset')  
  .split(' ')[0]
  .replace('_360', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.product-image-main img').length > 0){
    $arr = [];
    jQuery('.product-image-main img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('srcset')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

                            /////////////////////////////////////////////

  if (jQuery('.single-image img:first').length > 0) 
  $img = jQuery('.single-image img:first').attr('srcset')  
  .split(' ')[0]
  .replace('_60', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.single-image img:first').length > 0){
    $arr = [];
    jQuery('.single-image img:first').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('srcset')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

                                /////////////////////////////////////////////

  if (jQuery('.product-image-inner  img:first').length > 0) 
  $img = jQuery('.product-image-inner  img:first').attr('src')  
  .split(' ')[0]
  .replace('_60', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.product-image-inner  img').length > 0){
    $arr = [];
    jQuery('.product-image-inner  img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

                                  /////////////////////////////////////////////

  if (jQuery('.product__image-container  img:first').length > 0) 
  $img = jQuery('.product__image-container  img:first').attr('src')  
  .split(' ')[0]
  .replace('_60', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.product__image-container  img').length > 0){
    $arr = [];
    jQuery('.product__image-container  img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

                                     /////////////////////////////////////////////

  if (jQuery('.vis-container  img:first').length > 0) 
  $img = jQuery('.vis-container  img:first').attr('src')  
  .split(' ')[0]
  .replace('_60', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.vis-container  img').length > 0){
    $arr = [];
    jQuery('.vis-container  img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

   
    if (jQuery('.product_gallery  img:first').length > 0) 
    $img = jQuery('.product_gallery  img:first').attr('src')  
    .split(' ')[0]
    .replace('_60', '_1100')
    else
    $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
    
    if ($img.indexOf("http") == -1)
    'https:'+$img
    else
    $img
  
    if (jQuery('.product_gallery  img').length > 0){
      $arr = [];
      jQuery('.product_gallery img').each(function(index){
          if (index < 4) 
          $arr.push('https:'+jQuery(this).attr('src')  
          .split(' ')[0])
      })
      $arr
      }
  
      //////////////////////////////////////////////////

        //////////////////////////////////////////////////


    if (jQuery('[class="starting-slide slick-slide slick-current slick-active"] img:first').length > 0) 
    $img = jQuery('[class="starting-slide slick-slide slick-current slick-active"] img:first')  
    .attr('srcset') 
    .split(' ')[0]
    .replace('_180', '_1080')
    else
    $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
    
    if ($img.indexOf("http") == -1)
    'https:'+$img
    else
    $img
  
  
    
    
    if (jQuery('[class="starting-slide slick-slide slick-current slick-active"] img').length > 0){
      $arr = [];
      jQuery('[class="starting-slide slick-slide slick-current slick-active"] img').each(function(index){
          if (index < 4) 
          $arr.push('https:'+jQuery(this).attr("srcset"))
      })
      $arr
      }
  
    //////////////////////////////////////


     //////////////////////////////////////////////////

   
     if (jQuery('.product-gallery--media-wrapper  img:first').length > 0) 
     $img = jQuery('.product-gallery--media-wrapper  img:first').attr('src')  
     .split(' ')[0]
     .replace('_60', '_1100')
     else
     $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
   
     if (jQuery('.product-gallery--media-wrapper  img').length > 0){
       $arr = [];
       jQuery('.product-gallery--media-wrapper img').each(function(index){
           if (index < 4) 
           $arr.push('https:'+jQuery(this).attr('src')  
           .split(' ')[0])
       })
       $arr
       }
   
       //////////////////////////////////////////////////

       if (jQuery('[id="product-image-container"]  img:first').length > 0) 
     $img = jQuery('[id="product-image-container"]  img:first').attr('src')  
     .split(' ')[0]
     .replace('_60', '_1100')
     else
     $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
   
     if (jQuery('[id="product-image-container"]  img').length > 0){
       $arr = [];
       jQuery('[id="product-image-container"] img').each(function(index){
           if (index < 4) 
           $arr.push('https:'+jQuery(this).attr('src')  
           .split(' ')[0])
       })
       $arr
       }
   
       //////////////////////////////////////////////////
        //////////////////////////////////////////////////

   
     if (jQuery('.product-gallery__carousel-wrapper  img:first').length > 0) 
     $img = jQuery('.product-gallery__carousel-wrapper  img:first').attr('data-zoom')  
     .split(' ')[0]
     .replace('_60', '_1100')
     else
     $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
   
     if (jQuery('.product-gallery__carousel-wrapper  img').length > 0){
       $arr = [];
       jQuery('.product-gallery__carousel-wrapper img').each(function(index){
           if (index < 4) 
           $arr.push('https:'+jQuery(this).attr('data-zoom')  
           .split(' ')[0])
       })
       $arr
       }
   
       //////////////////////////////////////////////////
          //////////////////////////////////////////////////

   
     if (jQuery('.product-single__image-wrapper  img:first').length > 0) 
     $img = jQuery('.product-single__image-wrapper  img:first').attr('srcset')  
     .split(' ')[0]
     .replace('_180', '_1080')
     else
     $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
   
     if (jQuery('.product-single__image-wrapper  img').length > 0){
       $arr = [];
       jQuery('.product-single__image-wrapper img').each(function(index){
           if (index < 4) 
           $arr.push('https:'+jQuery(this).attr('srcset')  
           .split(' ')[0])
       })
       $arr
       }
   
       //////////////////////////////////////////////////
          //////////////////////////////////////////////////

   
     if (jQuery('.product-image-container  img:first').length > 0) 
     $img = jQuery('.product-image-container  img:first').attr('srcset')  
     .split(' ')[0]
     .replace('_180', '_1080')
     else
     $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
   
     if (jQuery('.product-image-container  img').length > 0){
       $arr = [];
       jQuery('.product-image-container img').each(function(index){
           if (index < 4) 
           $arr.push('https:'+jQuery(this).attr('srcset')  
           .split(' ')[0])
       })
       $arr
       }
   
       //////////////////////////////////////////////////

          //////////////////////////////////////////////////

   
     if (jQuery('.PD-galleryImages  img:first').length > 0) 
     $img = jQuery('.PD-galleryImages  img:first').attr('src')  
     .split(' ')[0]
     .replace('_180', '_1080')
     else
     $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
   
     if (jQuery('.PD-galleryImages  img').length > 0){
       $arr = [];
       jQuery('.PD-galleryImages img').each(function(index){
           if (index < 4) 
           $arr.push('https:'+jQuery(this).attr('src')  
           .split(' ')[0])
       })
       $arr
       }
   
       //////////////////////////////////////////////////
         //////////////////////////////////////////////////

         if (jQuery('[id="productSlideshow"]  img:first').length > 0) 
         $img = jQuery('[id="productSlideshow"]  img:first').attr('src')  
         .split(' ')[0]
         .replace('_60', '_1100')
         else
         $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
         
         if ($img.indexOf("http") == -1)
         'https:'+$img
         else
         $img
       
         if (jQuery('[id="productSlideshow"]  img').length > 0){
           $arr = [];
           jQuery('[id="productSlideshow"] img').each(function(index){
               if (index < 4) 
               $arr.push(
                //  'https:'+
                 jQuery(this).attr('src')  
               .split(' ')[0])
           })
           $arr
           }
       
           //////////////////////////////////////////////////
            //////////////////////////////////////////////////

    if (jQuery('[class="swiper-slide slick-slide slick-current slick-active"] img:first').length > 0) 
    $img = jQuery('[class="swiper-slide slick-slide slick-current slick-active"] img:first')  
    .attr('src') 
    .split(' ')[0]
    .replace('_180', '_1080')
    else
    $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
    
    if ($img.indexOf("http") == -1)
    'https:'+$img
    else
    $img

    
    if (jQuery('[class="swiper-slide slick-slide slick-current slick-active"] img').length > 0){
      $arr = [];
      jQuery('[class="swiper-slide slick-slide slick-current slick-active"] img').each(function(index){
          if (index < 4) 
          $arr.push('https:'+jQuery(this).attr("src"))
      })
      $arr
      }  
    //////////////////////////////////////
    
          //////////////////////////////////////////////////
   
          if (jQuery('.product-single__photo--container-thumb img:first').length > 0) 
          $img = jQuery('.product-single__photo--container-thumb img:first').attr('src')  
          .split(' ')[0]
          .replace('_180', '_1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.product-single__photo--container-thumb img').length > 0){
            $arr = [];
            jQuery('.product-single__photo--container-thumb img').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('src')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////
             //////////////////////////////////////////////////
   
          if (jQuery('.product-media--image img:first').length > 0) 
          $img = jQuery('.product-media--image img:first').attr('srcset')  
          .split(' ')[0]
          .replace('_180', '_1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.product-media--image img:first').length > 0){
            $arr = [];
            jQuery('.product-media--image img:first').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('srcset')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////
               //////////////////////////////////////////////////
   
          if (jQuery('.product-image-slider-wrapper img:last').length > 0) 
          $img = jQuery('.product-image-slider-wrapper img:last').attr('src')  
          .split(' ')[0]
          .replace('_180', '_1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.product-image-slider-wrapper img').length > 0){
            $arr = [];
            jQuery('.product-image-slider-wrapper img').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('src')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////
               //////////////////////////////////////////////////
   
          if (jQuery('.swiper-wrapper img:first').length > 0) 
          $img = jQuery('.swiper-wrapper img:first').attr('data-srczoom')  
          .split(' ')[0]
          .replace('_180', '_1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.swiper-wrapper img').length > 0){
            $arr = [];
            jQuery('.swiper-wrapper img').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('src')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////

               //////////////////////////////////////////////////
   
          if (jQuery('.product-page--image-container img:first').length > 0) 
          $img = jQuery('.product-page--image-container img:first').attr('srcset')  
          .split(' ')[0]
          .replace('_295', '_1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.product-page--image-container img:first').length > 0){
            $arr = [];
            jQuery('.product-page--image-container img:first').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('srcset')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////
                 //////////////////////////////////////////////////
   
          if (jQuery('.product-single__photo-wrapper img:first').length > 0) 
          $img = jQuery('.product-single__photo-wrapper img:first').attr('src')  
          .split(' ')[0]
          .replace('_300x300', '_1080x1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.product-single__photo-wrapper img:first').length > 0){
            $arr = [];
            jQuery('.product-single__photo-wrapper img:first').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('src')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////

                        //////////////////////////////////////////////////
   
          if (jQuery('.product-single__photo__wrapper img:first').length > 0) 
          $img = jQuery('.product-single__photo__wrapper img:first').attr('src')  
          .split(' ')[0]
          .replace('_300x', '_1080x')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.product-single__photo__wrapper img:first').length > 0){
            $arr = [];
            jQuery('.product-single__photo__wrapper img:first').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('src')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////
                  //////////////////////////////////////////////////
   
          if (jQuery('.product-page-gallery__main img:first').length > 0) 
          $img = jQuery('.product-page-gallery__main img:first').attr('src')  
          .split(' ')[0]
          .replace('_180', '_1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.product-page-gallery__main img:first').length > 0){
            $arr = [];
            jQuery('.product-page-gallery__main img:first').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('src')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////
                 //////////////////////////////////////////////////

    if (jQuery('[class="image slick-slide slick-current slick-active"] img:first').length > 0) 
    $img = jQuery('[class="image slick-slide slick-current slick-active"] img:first')  
    .attr('src') 
    .split(' ')[0]
    .replace('_180', '_1080')
    else
    $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
    
    if ($img.indexOf("http") == -1)
    'https:'+$img
    else
    $img

    
    if (jQuery('[class="image slick-slide slick-current slick-active"] img').length > 0){
      $arr = [];
      jQuery('[class="image slick-slide slick-current slick-active"] img').each(function(index){
          if (index < 4) 
          $arr.push('https:'+jQuery(this).attr("src"))
      })
      $arr
      }  
    //////////////////////////////////////
           //////////////////////////////////////////////////   
           if (jQuery('.product-page--images-container img:first').length > 0) 
           $img = jQuery('.product-page--images-container img:first').attr('srcset')  
           .split(' ')[0]
           .replace('_295', '_1080')
           else
           $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
           
           if ($img.indexOf("http") == -1)
           'https:'+$img
           else
           $img
         
           if (jQuery('.product-page--images-container img:first').length > 0){
             $arr = [];
             jQuery('.product-page--images-container img:first').each(function(index){
                 if (index < 4) 
                 $arr.push('https:'+jQuery(this).attr('srcset')  
                 .split(' ')[0])
             })
             $arr
             }        
                //////////////////////////////////////////////////   

                    //////////////////////////////////////////////////   
           if (jQuery('.product-page-images-container img:first').length > 0) 
           $img = jQuery('.product-page-images-container img:first').attr('srcset')  
           .split(' ')[0]
           .replace('_295', '_1080')
           else
           $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
           
           if ($img.indexOf("http") == -1)
           'https:'+$img
           else
           $img
         
           if (jQuery('.product-page-images-container img:first').length > 0){
             $arr = [];
             jQuery('.product-page-images-container img:first').each(function(index){
                 if (index < 4) 
                 $arr.push('https:'+jQuery(this).attr('srcset')  
                 .split(' ')[0])
             })
             $arr
             }        
                //////////////////////////////////////////////////   
                   //////////////////////////////////////////////////

                   if (jQuery('[class="tns-item tns-slide-active"] img:first').length > 0) 
                   $img = jQuery('[class="tns-item tns-slide-active"] img:first')  
                   .attr('src') 
                   .split(' ')[0]
                   .replace('_180', '_1080')
                   else
                   $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
                   
                   if ($img.indexOf("http") == -1)
                   'https:'+$img
                   else
                   $img
               
                   
                   if (jQuery('[class="tns-item tns-slide-active"] img').length > 0){
                     $arr = [];
                     jQuery('[class="tns-item tns-slide-active"] img').each(function(index){
                         if (index < 4) 
                         $arr.push('https:'+jQuery(this).attr("src"))
                     })
                     $arr
                     }  
                   //////////////////////////////////////   
                      //////////////////////////////////////////////////

                   if (jQuery('[class="product-single__photo-container slick-slide slick-current slick-active"] img:first').length > 0) 
                   $img = jQuery('[class="product-single__photo-container slick-slide slick-current slick-active"] img:first')  
                   .attr('src') 
                   .split(' ')[0]
                   .replace('_100', '_1080')
                   else
                   $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
                   
                   if ($img.indexOf("http") == -1)
                   'https:'+$img
                   else
                   $img
               
                   
                   if (jQuery('[class="product-single__photo-container slick-slide slick-current slick-active"] img').length > 0){
                     $arr = [];
                     jQuery('[class="product-single__photo-container slick-slide slick-current slick-active"] img').each(function(index){
                         if (index < 4) 
                         $arr.push('https:'+jQuery(this).attr("src"))
                     })
                     $arr
                     }  
                   //////////////////////////////////////          
                     //////////////////////////////////////////////////   
           if (jQuery('.product-detail-provider-wrapper .shop-panel__image-wrapper img:first').length > 0           ) 
           $img = jQuery('.product-detail-provider-wrapper .shop-panel__image-wrapper img:first').attr('src')  
           .split(' ')[0]
           .replace('_295', '_1080')
          //  else
          //  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
           
           if ($img.indexOf("http") == -1)
           'https:'+$img
           else
           $img
         
           if (jQuery('.shop-panel__image-wrapper img:first').length > 0){
             $arr = [];
             jQuery('.shop-panel__image-wrapper img:first').each(function(index){
                 if (index < 4) 
                 $arr.push(jQuery(this).attr('src')  
                 .split(' ')[0])
             })
             $arr
             }        
                //////////////////////////////////////////////////   
     //////////////////////////////////////////////////   
     if (jQuery('.Product__Gallery img:first').length > 0) 
     $img = jQuery('.Product__Gallery img:first').attr('data-original-src')  
     .split(' ')[0]
     .replace('_295', '_1080')
     else
     $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
   
     if (jQuery('.Product__Gallery img:first').length > 0){
       $arr = [];
       jQuery('.Product__Gallery img:first').each(function(index){
           if (index < 4) 
           $arr.push('https:'+jQuery(this).attr('data-original-src')  
           .split(' ')[0])
       })
       $arr
       }        
          //////////////////////////////////////////////////   
           //////////////////////////////////////////////////   
     if (jQuery('[id="section-product"] img:first').length > 0) 
     $img = jQuery('.product-single img:first').attr('src')  
     .split(' ')[0]
     .replace('_295', '_1080')
     else
     $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
   
     if (jQuery('.product-single img:first').length > 0){
       $arr = [];
       jQuery('.product-single img:first').each(function(index){
           if (index < 4) 
           $arr.push('https:'+jQuery(this).attr('src')  
           .split(' ')[0])
       })
       $arr
       }        
          //////////////////////////////////////////////////   
                //////////////////////////////////////////////////   
     if (jQuery('.product-single img:first').length > 0) 
     $img = jQuery('[id="section-product"] img:first').attr('src')  
     .split(' ')[0]
     .replace('_295', '_1080')
     else
     $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
   
     if (jQuery('[id="section-product"] img:first').length > 0){
       $arr = [];
       jQuery('[id="section-product"] img:first').each(function(index){
           if (index < 4) 
           $arr.push('https:'+jQuery(this).attr('src')  
           .split(' ')[0])
       })
       $arr
       }        
          //////////////////////////////////////////////////   

   //////////////////////////////////////////////////

   if (jQuery('[class="slick-slide slick-current slick-active"] img:first').length > 0) 
   $img = jQuery('[class="slick-slide slick-current slick-active"] img:first')  
   .attr('src') 
   .split(' ')[0]
   .replace('_180', '_1080')
   else
   $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
   
   if ($img.indexOf("http") == -1)
   'https:'+$img
   else
   $img

   
   if (jQuery('[class="slick-slide slick-current slick-active"] img').length > 0){
     $arr = [];
     jQuery('[class="slick-slide slick-current slick-active"] img').each(function(index){
         if (index < 4) 
         $arr.push('https:'+jQuery(this).attr("src"))
     })
     $arr
     }  
   //////////////////////////////////////   
              //////////////////////////////////////////////////   
              if (jQuery('.product-images img:first').length > 0) 
              $img = jQuery('.product-images img:first').attr('srcset')  
              .split(' ')[0]
              .replace('_85', '_1080')
              else
              $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
              
              if ($img.indexOf("http") == -1)
              'https:'+$img
              else
              $img
            
              if (jQuery('.product-images img:first').length > 0){
                $arr = [];
                jQuery('.product-images img:first').each(function(index){
                    if (index < 4) 
                    $arr.push('https:'+jQuery(this).attr('srcset')  
                    .split(' ')[0])
                })
                $arr
                }        
                   //////////////////////////////////////////////////   
      //////////////////////////////////////////////////   
      if (jQuery('.product-media-container img:first').length > 0) 
      $img = jQuery('.product-media-container img:first').attr('srcset')  
      .split(' ')[0]
      .replace('_180', '_1080')
      else
      $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
      
      if ($img.indexOf("http") == -1)
      'https:'+$img
      else
      $img
    
      if (jQuery('.product-media-container img').length > 0){
        $arr = [];
        jQuery('.product-media-container img').each(function(index){
            if (index < 4) 
            $arr.push('https:'+jQuery(this).attr('srcset')  
            .split(' ')[0])
        })
        $arr
        }        
           //////////////////////////////////////////////////   
  //////////////////////////////////////////////////

  if (jQuery('[id="rondellCarousel"] img:first').length > 0) 
  $img = jQuery('[id="rondellCarousel"] img:first')  
  .attr('src') 
  .split(' ')[0]
  .replace('_180', '_1080')
  else
  $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  
  if (jQuery('[id="rondellCarousel"] img').length > 0){
    $arr = [];
    jQuery('[id="rondellCarousel"] img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr("src"))
    })
    $arr
    }  
  //////////////////////////////////////   
    //////////////////////////////////////////////////   
    if (jQuery('.product-media-wrapper img:first').length > 0) 
    $img = jQuery('.product-media-wrapper img:first').attr('srcset')  
    .split(' ')[0]
    .replace('_180', '_1080')
    else
    $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
    
    if ($img.indexOf("http") == -1)
    'https:'+$img
    else
    $img
  
    if (jQuery('.product-media-wrapper img').length > 0){
      $arr = [];
      jQuery('.product-media-wrapper img').each(function(index){
          if (index < 4) 
          $arr.push('https:'+jQuery(this).attr('srcset')  
          .split(' ')[0])
      })
      $arr
      }        
         //////////////////////////////////////////////////   
         //////////////////////////////////////////////////

  if (jQuery('[id="product-gallery"] img:first').length > 0) 
  $img = jQuery('[id="product-gallery"] img:first')  
  .attr('src') 
  .split(' ')[0]
  .replace('_180', '_1080')
  else
  $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  
  if (jQuery('[id="product-gallery"] img').length > 0){
    $arr = [];
    jQuery('[id="product-gallery"] img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr("src"))
    })
    $arr
    }  
  //////////////////////////////////////   
  //////////////////////////////////////////////////   
  if (jQuery('.productMainImage img:first').length > 0) 
  $img = jQuery('.productMainImage img:first').attr('src')  
  .split(' ')[0]
  .replace('_180', '_1080')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.productMainImage img').length > 0){
    $arr = [];
    jQuery('.productMainImage img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }        
       //////////////////////////////////////////////////   
//////////////////////////////////////////////////   
if (jQuery('.product-single__media-wrapper img:first').length > 0) 
$img = jQuery('.product-single__media-wrapper img:first').attr('srcset')  
.split(' ')[0]
.replace('_180', '_1080')
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img

if (jQuery('.product-single__media-wrapper img:first').length > 0){
  $arr = [];
  jQuery('.product-single__media-wrapper img:first').each(function(index){
      if (index < 4) 
      $arr.push('https:'+jQuery(this).attr('srcset')  
      .split(' ')[0])
  })
  $arr
  }        
     //////////////////////////////////////////////////   
     ///////////////////
     if (jQuery('.gallery-container img:first').length > 0) 
     $img = jQuery('.gallery-container img:first').attr('srcset')
     .split(' ')[0]
     .replace('_180', '_1024')
     if (jQuery('.product-carousel img:first').length > 0) 
     $img = jQuery('.product-carousel img:first').attr('srcset')
     .split(' ')[0]
     .replace('_180', '_1024')
     else
     $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
     /////////////////////////////////////////////////////
     ////////////////
     if (jQuery('.product-image img:first').length > 0) 
     $img = jQuery('.product-image img:first').attr('data-zoom-src')  
     .split(' ')[0]
     .replace('_60', '_1100')
     else
     $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
   
     if (jQuery('.product-image img').length > 0){
       $arr = [];
       jQuery('.product-image img').each(function(index){
           if (index < 4) 
           $arr.push('https:'+jQuery(this).attr('data-zoom-src')  
           .split(' ')[0])
       })
       $arr
       }
       //////////////////////////////////////////////////////
              //////////////////////////////////////////////////

  if (jQuery('[id="main_photo_slider"] a:first').length > 0) 
  $a = jQuery('[id="main_photo_slider"] a:first')  
  .attr('href') 
  .split(' ')[0]
  .replace('_180', '_1080')
  else
  $a = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
  
  if ($a.indexOf("http") == -1)
  'https:'+$a
  else
  $a

  
  if (jQuery('[id="main_photo_slider"] a').length > 0){
    $arr = [];
    jQuery('[id="main_photo_slider"] a').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr("href"))
    })
    $arr
    }  
  //////////////////////////////////////   
  /////////////////////////
  if (jQuery('.zoomWindowContainer div:first').length > 0) 
$img = jQuery(".zoomWindowContainer div:first").css('background-image').slice(5, -2)
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img

if (jQuery('.product-single__thumbnail-item img').length > 0){
  $arr = [];
  jQuery('.product-single__thumbnail-item img').each(function(index){
      if (index < 4) 
      $arr.push('https:'+jQuery(this).attr('src')  
      .split(' ')[0])
  })
  $arr
  }
  /////////////////////////////////////////
           /////////////////////////////////////////////

           if (jQuery('.product_section .description').length > 0) 
           $img =
           'https:'+
           jQuery('.product_section img:first').attr('src')  
           .split(' ')[0]
           .replace('_60', '_1100')
          //  else
          //  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
           
           if ($img.indexOf("http") == -1)
           'https:'+$img
           else
           $img
         
           if (jQuery('.product_section img').length > 0){
             $arr = [];
             jQuery('.product_section img').each(function(index){
                 if (index < 4) 
                 $arr.push('https:'+jQuery(this).attr('src')  
                 .split(' ')[0])
             })
             $arr
             }
         
             //////////////////////////////////////////////////
     ////////////////////////////////

  if (jQuery('[class="starting-slide slick-slide slick-current slick-active"] img:first').length > 0) 
  $img = jQuery('[class="starting-slide slick-slide slick-current slick-active"] img:first')  
  .attr('srcset') 
  .split(' ')[0]
  .replace('_180', '_1080')
  else
  $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img  
  
  if (jQuery('[class="starting-slide slick-slide slick-current slick-active"] img').length > 0){
    $arr = [];
    jQuery('[class="starting-slide slick-slide slick-current slick-active"] img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr("srcset"))
    })
    $arr
    }

  //////////////////////////////////////
          /////////////////////////////////////////////

          if (jQuery('.product-single img:first').length > 0) 
          $img = jQuery('.product-single img:first').attr('src')  
          .split(' ')[0]
          .replace('_150x150', '_1080x1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.product-single img').length > 0){
            $arr = [];
            jQuery('.product-single img').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('src')  
                .split(' ')[0])
            })
            $arr
            }
        
            //////////////////////////////////////////////////
             //////////////////////////////////////////////////

       if (jQuery('[id="product_images"]  img:first').length > 0) 
       $img = jQuery('[id="product_images"]  img:first').attr('src')  
       .split(' ')[0]
       .replace('_150', '_1100')
       else
       $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
       
       if ($img.indexOf("http") == -1)
       'https:'+$img
       else
       $img
     
       if (jQuery('[id="product_images"]  img').length > 0){
         $arr = [];
         jQuery('[id="product_images"] img').each(function(index){
             if (index < 4) 
             $arr.push('https:'+jQuery(this).attr('src')  
             .split(' ')[0])
         })
         $arr
         }
     
         //////////////////////////////////////////////////
            /////////////////////////////////////////////
            if (jQuery('.css-1b25yh7 img:first').length > 0) 
            $img = jQuery('.css-1b25yh7 img:first').attr('src')  
            .split(' ')[0]
            .replace('_150x150', '_1080x1080')
            else
            $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
            
            if ($img.indexOf("http") == -1)
            'https:'+$img
            else
            $img
          
            if (jQuery('.css-1b25yh7 img').length > 0){
              $arr = [];
              jQuery('.css-1b25yh7 img').each(function(index){
                  if (index < 4) 
                  $arr.push('https:'+jQuery(this).attr('src')  
                  .split(' ')[0])
              })
              $arr
              }          
              //////////////////////////////////////////////////
                  //////////////////////////////////////////////////
   
          if (jQuery('.MagicToolboxContainer img:first').length > 0) 
          $img = jQuery('.MagicToolboxContainer img:first').attr('src')  
          .split(' ')[0]
          .replace('_300x300', '_1080x1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.MagicToolboxContainer img:first').length > 0){
            $arr = [];
            jQuery('.MagicToolboxContainer img:first').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('src')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////
              //////////////////////////////////////////////////
   
          if (jQuery('.cloud-zoom img:first').length > 0) 
          $img = jQuery('.cloud-zoom img:first').attr('src')  
          .split(' ')[0]
          .replace('_180', '_1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.cloud-zoom img').length > 0){
            $arr = [];
            jQuery('.cloud-zoom img').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('src')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////
              //////////////////////////////////////////////////
   
          if (jQuery('.product__photo--single img:first').length > 0) 
          $img = jQuery('.product__photo--single img:first').attr('src')  
          .split(' ')[0]
          .replace('_180', '_1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.product__photo--single img').length > 0){
            $arr = [];
            jQuery('.product__photo--single img').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('src')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////
               //////////////////////////////////////////////////
   
          if (jQuery('.product-main-image img:first').length > 0) 
          $img = jQuery('.product-main-image img:first').attr('src')  
          .split(' ')[0]
          .replace('_180', '_1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.product-main-image img').length > 0){
            $arr = [];
            jQuery('.product-main-image img').each(function(index){
                if (index < 4) 
                $arr.push(jQuery(this).attr('src')  
                .split(' ')[0])
            })
            $arr
            }        
            //////////////////////////////////////////////////
            ////////////////////////////////////////////////////

if (jQuery('.sep-loaded-slider img:first').length > 0) 
$img = jQuery('.sep-loaded-slider img:first').attr('srcset')
.split(' ')[0]
.replace('_180', '_1080')
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('content')


if ($img.indexOf("http") == -1)
'https:'+$img
else
$img


if (jQuery('.sep-loaded-slider img').length > 0){
  $arr = [];
  jQuery('.sep-loaded-slider img').each(function(index){
      if (index < 4) 
      $arr.push('https:'+jQuery(this).attr("srcset"))
  })
  $arr
  }

//////////////////////
            ////////////////////////////////////////////////////

            if (jQuery('.product__main-photos img:first').length > 0) 
            $img = jQuery('.product__main-photos img:first').attr('srcset')
            .split(' ')[0]
            .replace('_180', '_1080')
            else
            $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
            
            
            if ($img.indexOf("http") == -1)
            'https:'+$img
            else
            $img
            
            
            if (jQuery('.product__main-photos img').length > 0){
              $arr = [];
              jQuery('.product__main-photos img').each(function(index){
                  if (index < 4) 
                  $arr.push('https:'+jQuery(this).attr("srcset"))
              })
              $arr
              }
            
            //////////////////////

          ////////////////////////////////////////////////////

          if (jQuery('.product__main-photos-wrapper img:first').length > 0) 
          $img = jQuery('.product__main-photos-wrapper img:first').attr('srcset')
          .split(' ')[0]
          .replace('_180', '_1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
          
          
          if (jQuery('.product__main-photos-wrapper img').length > 0){
            $arr = [];
            jQuery('.product__main-photos-wrapper img').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr("srcset"))
            })
            $arr
            }
          
          //////////////////////
           ////////////////////////////////////////////////////

           if (jQuery('.data-zoom-wrapper img:first').length > 0) 
           $img = jQuery('.data-zoom-wrapper img:first').attr('data-src')
           
           .replace('_180', '_1080')
           else
           $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
           
           
           if ($img.indexOf("http") == -1)
           'https:'+$img
           else
           $img
           
           
           if (jQuery('.data-zoom-wrapper img').length > 0){
             $arr = [];
             jQuery('.data-zoom-wrapper img').each(function(index){
                 if (index < 4) 
                 $arr.push('https:'+jQuery(this).attr("data-src").split(' ')[0])
             })
             $arr
             }
           
           //////////////////////
             ////////////////////////////////////////////////////

             if (jQuery('.product-single__image-wrapper img:first').length > 0) 
             $img = jQuery('.product-single__image-wrapper img:first').attr('src')
             .split(' ')[0]
             .replace('_180', '_1080')
             else
             $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
             
             
             if ($img.indexOf("http") == -1)
             'https:'+$img
             else
             $img
             
             
             if (jQuery('.product-single__image-wrapper img').length > 0){
               $arr = [];
               jQuery('.product-single__image-wrapper img').each(function(index){
                   if (index < 4) 
                   $arr.push('https:'+jQuery(this).attr("src"))
               })
               $arr
               }
             
             //////////////////////
                  ////////////////////////////////

  if (jQuery('[class="gallery-cell is-selected"] img:first').length > 0) 
  $img = jQuery('[class="gallery-cell is-selected"] img:first')  
  .attr('src') 
  .split(' ')[0]
  .replace('_180', '_1080')
  else
  $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img  
  
  if (jQuery('[class="gallery-cell is-selected"] img').length > 0){
    $arr = [];
    jQuery('[class="gallery-cell is-selected"] img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr("src"))
    })
    $arr
    }

  //////////////////////////////////////
     ////////////////////////////////////////////////////

     if (jQuery('.product-content img:first').length > 0) 
     $img = jQuery('.product-content img:first').attr('src')
     .split(' ')[0]
     .replace('_180', '_1080')
     else
     $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
     
     
     if ($img.indexOf("http") == -1)
     'https:'+$img
     else
     $img
     
     
     if (jQuery('.product-content img').length > 0){
       $arr = [];
       jQuery('.product-content img').each(function(index){
           if (index < 4) 
           $arr.push('https:'+jQuery(this).attr("src"))
       })
       $arr
       }
     
     //////////////////////

       ////////////////////////////////////////////////////

       if (jQuery('.main-media-image-wrapper-hook img:first').length > 0) 
       $img = jQuery('.main-media-image-wrapper-hook img:first').attr('src')
       .split(' ')[0]
       .replace('_180', '_1080')
       else
       $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
       
       
       if ($img.indexOf("http") == -1)
       'https:'+$img
       else
       $img
       
       
       if (jQuery('.main-media-image-wrapper-hook img').length > 0){
         $arr = [];
         jQuery('.main-media-image-wrapper-hook img').each(function(index){
             if (index < 4) 
             $arr.push('https:'+jQuery(this).attr("src"))
         })
         $arr
         }
       
       //////////////////////
                         ////////////////////////////////

  if (jQuery('[class="product-slide swiper-slide swiper-slide-active"] img:first').length > 0) 
  $img = jQuery('[class="product-slide swiper-slide swiper-slide-active"] img:first')  
  .attr('srcset') 
  .split(' ')[0]
  .replace('_180', '_1080')
  else
  $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img  
  
  if (jQuery('[class="product-slide swiper-slide swiper-slide-active"] img').length > 0){
    $arr = [];
    jQuery('[class="product-slide swiper-slide swiper-slide-active"] img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr("srcset"))
    })
    $arr
    }

  //////////////////////////////////////

    //////////////////////////////////////////////////

    if (jQuery('[id="product-photos"] img:first').length > 0) 
    $img = jQuery('[id="product-photos"] img:first')  
    .attr('src') 
    .split(' ')[0]
    .replace('_180', '_1080')
    else
    $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
    
    if ($img.indexOf("http") == -1)
    'https:'+$img
    else
    $img
 
    
    if (jQuery('[id="product-photos"] img').length > 0){
      $arr = [];
      jQuery('[id="product-photos"] img').each(function(index){
          if (index < 4) 
          $arr.push('https:'+jQuery(this).attr("src"))
      })
      $arr
      }  
    //////////////////////////////////////   
          /////////////////////////////////////////////

          if (jQuery('.images .is-selected').length > 0) 
          $img = jQuery('.images .is-selected').attr('data-src')  
          .split(' ')[0]
          // .replace('_150x150', '_1080x1080')
          else
          $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
          
          if ($img.indexOf("http") == -1)
          'https:'+$img
          else
          $img
        
          if (jQuery('.images .is-selected').length > 0){
            $arr = [];
            jQuery('.images .is-selected').each(function(index){
                if (index < 4) 
                $arr.push('https:'+jQuery(this).attr('data-src')  
                .split(' ')[0])
            })
            $arr
            }
        
            //////////////////////////////////////////////////

            if (jQuery('.product-image-zoom img:first').length > 0) 
$img = 
// 'https://www.deluxdeco.co.uk/' + 
jQuery('.product-image-zoom img:first').attr('src')
else
$img = jQuery('meta[property="og:image"]:eq(0)').attr('content')

if ($img.indexOf("http") == -1)
'https:'+$img
else
$img

if (jQuery('.product-image-zoom img').length > 0){
  $arr = [];
  jQuery('.product-image-zoom img').each(function(index){
      if (index < 4) 
      $arr.push(jQuery(this).attr('src')  
      .split(' ')[0])
  })
  $arr
  }

  //////////////////////////////////////////////////
    ////////////////////////////////////////////////////

    if (jQuery('.zoom-small-image img:first').length > 0) 
    $img = jQuery('.zoom-small-image img:first').attr('src')
    .split(' ')[0]
    .replace('_180', '_1080')
    else
    $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
    
    
    if ($img.indexOf("http") == -1)
    'https:'+$img
    else
    $img
    
    
    if (jQuery('.zoom-small-image img').length > 0){
      $arr = [];
      jQuery('.zoom-small-image img').each(function(index){
          if (index < 4) 
          $arr.push(jQuery(this).attr("src"))
      })
      $arr
      }
    
    //////////////////////

      ////////////////////////////////////////////////////

      if (jQuery('.product-page--media img:first').length > 0) 
      $img = jQuery('.product-page--media img:first').attr('srcset')
      .split(' ')[0]
      .replace('_180', '_1080')
      else
      $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
      
      
      if ($img.indexOf("http") == -1)
      'https:'+$img
      else
      $img
      
      
      if (jQuery('.product-page--media img:first').length > 0){
        $arr = [];
        jQuery('.product-page--media img:first').each(function(index){
            if (index < 4) 
            $arr.push('https:'+jQuery(this).attr("srcset"))
        })
        $arr
        }
      
      //////////////////////
                 /////////////////////////////////////////////

  if (jQuery('.product-image-wrap img:first').length > 0) 
  $img = jQuery('.product-image-wrap img:first').attr('src')  
  .split(' ')[0]
  .replace('_60', '_1100')
  else
  $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img

  if (jQuery('.product-image-wrap img').length > 0){
    $arr = [];
    jQuery('.product-image-wrap img').each(function(index){
        if (index < 4) 
        $arr.push(jQuery(this).attr('src')  
        .split(' ')[0])
    })
    $arr
    }

    //////////////////////////////////////////////////

       ////////////////////////////////

  if (jQuery('[data-pf-type="ProductImage2"] img:first').length > 0) 
  $img = jQuery('[data-pf-type="ProductImage2"] img:first')  
  .attr('src') 
  .split(' ')[0]
  .replace('_180', '_1080')
  else
  $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img  
  
  if (jQuery('[data-pf-type="ProductImage2"] img').length > 0){
    $arr = [];
    jQuery('[data-pf-type="ProductImage2"] img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr("src"))
    })
    $arr
    }

  //////////////////////////////////////

          ////////////////////////////////

  if (jQuery('[id="shopify-section-Product-Video-Gallery"] img:first').length > 0) 
  $img = jQuery('[id="shopify-section-Product-Video-Gallery"] img:first')  
  .attr('src') 
  .split(' ')[0]
  .replace('_180', '_1080')
  else
  $img = jQuery('.meta[property="og:image"]:eq(0)').attr('content')
  
  if ($img.indexOf("http") == -1)
  'https:'+$img
  else
  $img  
  
  if (jQuery('[id="shopify-section-Product-Video-Gallery"] img').length > 0){
    $arr = [];
    jQuery('[id="shopify-section-Product-Video-Gallery"] img').each(function(index){
        if (index < 4) 
        $arr.push('https:'+jQuery(this).attr("src"))
    })
    $arr
    }

  //////////////////////////////////////
              /////////////////////////////////////////////product-gallery__image is-selected

              if (jQuery('.product-gallery__image img:first').length > 0) 
              $img = jQuery('.product-gallery__image img:first').attr('src')  
              .split(' ')[0]
              .replace('_60', '_1100')
              else
              $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
              
              if ($img.indexOf("http") == -1)
              'https:'+$img
              else
              $img
            
              if (jQuery('.product-gallery__image img').length > 0){
                $arr = [];
                jQuery('.product-gallery__image img').each(function(index){
                    if (index < 4) 
                    $arr.push("http"+jQuery(this).attr('src')  
                    .split(' ')[0])
                })
                $arr
                }
            
                //////////////////////////////////////////////////

                              /////////////////////////////////////////////product-gallery__image is-selected

              if (jQuery('.product-gallery--image img:first').length > 0) 
              $img = jQuery('.product-gallery--image img:first').attr('src')  
              .split(' ')[0]
              .replace('_60', '_1100')
              else
              $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
              
              if ($img.indexOf("http") == -1)
              'https:'+$img
              else
              $img
            
              if (jQuery('.product-gallery--image img').length > 0){
                $arr = [];
                jQuery('.product-gallery--image img').each(function(index){
                    if (index < 4) 
                    $arr.push("http"+jQuery(this).attr('src')  
                    .split(' ')[0])
                })
                $arr
                }
            
                //////////////////////////////////////////////////
                                             /////////////////////////////////////////////product-gallery__image is-selected

              if (jQuery('.Product__SlideItem--image img:first').length > 0) 
              $img = jQuery('.Product__SlideItem--image img:first').attr('data-original-src')  
              .split(' ')[0]
              .replace('_200x', '_1080x')
              else
              $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
              
              if ($img.indexOf("http") == -1)
              'https:'+$img
              else
              $img
            
              if (jQuery('.Product__SlideItem--image img:first').length > 0){
                $arr = [];
                jQuery('.Product__SlideItem--image img:first').each(function(index){
                    if (index < 4) 
                    $arr.push("http"+jQuery(this).attr('data-original-src')  
                    .split(' ')[0])
                })
                $arr
                }
            
                //////////////////////////////////////////////////

                                                             /////////////////////////////////////////////product-gallery__image is-selected

              if (jQuery('.gallery__image img:first').length > 0) 
              $img = jQuery('.gallery__image img:first').attr('srcset')  
              .split(' ')[0]
              .replace('_180x', '_1080x')
              else
              $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
              
              if ($img.indexOf("http") == -1)
              'https:'+$img
              else
              $img
            
              if (jQuery('.gallery__image img:first').length > 0){
                $arr = [];
                jQuery('.gallery__image img:first').each(function(index){
                    if (index < 4) 
                    $arr.push("http"+jQuery(this).attr('srcset')  
                    .split(' ')[0])
                })
                $arr
                }
            
                //////////////////////////////////////////////////
                                                                             /////////////////////////////////////////////product-gallery__image is-selected

              if (jQuery('.product__media-container img:first').length > 0) 
              $img = jQuery('.product__media-container img:first').attr('srcset')  
              .split(' ')[0]
              .replace('_360x', '_1080x')
              else
              $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
              
              if ($img.indexOf("http") == -1)
              'https:'+$img
              else
              $img
            
              if (jQuery('.product__media-container img:first').length > 0){
                $arr = [];
                jQuery('.product__media-container img:first').each(function(index){
                    if (index < 4) 
                    $arr.push("http"+jQuery(this).attr('srcset')  
                    .split(' ')[0])
                })
                $arr
                }
            
                //////////////////////////////////////////////////
                                                                                /////////////////////////////////////////////product-gallery__image is-selected

              if (jQuery('.product_images img:first').length > 0) 
              $img = jQuery('.product_images img:first').attr('srcset')  
              .split(' ')[0]
              .replace('_180x', '_1080x')
              else
              $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
              
              if ($img.indexOf("http") == -1)
              'https:'+$img
              else
              $img
            
              if (jQuery('.product_images img:first').length > 0){
                $arr = [];
                jQuery('.product_images img:first').each(function(index){
                    if (index < 4) 
                    $arr.push("http"+jQuery(this).attr('srcset')  
                    .split(' ')[0])
                })
                $arr
                }
            
                //////////////////////////////////////////////////
                                                                                 /////////////////////////////////////////////product-gallery__image is-selected

              if (jQuery('.product-image-list img:first').length > 0) 
              $img = jQuery('.product-image-list img:first').attr('src')  
              .split(' ')[0]
              .replace('_180x', '_1080x')
              else
              $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
              
              if ($img.indexOf("http") == -1)
              'https:'+$img
              else
              $img
            
              if (jQuery('.product-image-list img:first').length > 0){
                $arr = [];
                jQuery('.product-image-list img:first').each(function(index){
                    if (index < 4) 
                    $arr.push("http"+jQuery(this).attr('src')  
                    .split(' ')[0])
                })
                $arr
                }
            
                //////////////////////////////////////////////////
              ////////////////////////////////////////////

              if (jQuery('.proBoxImage a:first').length > 0) 
              $img = jQuery('.proBoxImage a:first').attr('href')
              // attr('src')  
              .split(' ')[0]
              .replace('_180x', '_1080x')
              else
              $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
              
              if ($img.indexOf("http") == -1)
              'https:'+$img
              else
              $img
            
              if (jQuery('.proBoxImage a:first').length > 0){
                $arr = [];
                jQuery('.proBoxImage a:first').each(function(index){
                    if (index < 4) 
                    $arr.push("http"+jQuery(this).attr('href')
                    // attr('src')  
                    .split(' ')[0])
                })
                $arr
                }
                //////////////////////////////////////////////////
                 ////////////////////////////////////////////

              if (jQuery('.product-image:first').length > 0) 
              $img = jQuery('.product-image:first')
              .attr('data-zoom-image')  
              .split(' ')[0]
              .replace('_180x', '_1080x')
              else
              $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
              
              if ($img.indexOf("http") == -1)
              'https:'+$img
              else
              $img
            
              if (jQuery('.product-image:first').length > 0){
                $arr = [];
                jQuery('.product-image:first').each(function(index){
                    if (index < 4) 
                    $arr.push("http"+jQuery(this)
                    .attr('data-zoom-image')  
                    .split(' ')[0])
                })
                $arr
                }
                //////////////////////////////////////////////////
               /////////////////////////////////////////////

              if (jQuery('.tt-product-single-img img:first').length > 0) 
              $img = jQuery('.tt-product-single-img img:first').attr('src')  
              .split(' ')[0]
              .replace('_180x', '_1080x')
              else
              $img = jQuery('meta[property="og:image"]:eq(0)').attr('content')
              
              if ($img.indexOf("http") == -1)
              'https:'+$img
              else
              $img
            
              if (jQuery('.tt-product-single-img img:first').length > 0){
                $arr = [];
                jQuery('.tt-product-single-img img:first').each(function(index){
                    if (index < 4) 
                    $arr.push("http"+jQuery(this).attr('src')  
                    .split(' ')[0])
                })
                $arr
                }
            
                //////////////////////////////////////////////////